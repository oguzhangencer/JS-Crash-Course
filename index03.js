// arrow function

console.log(sum(2, 4)) //  it works

function sum (a, b){
  return a + b
}

//
//
//

console.log(power(2, 3)) // it does not work

const power = (a, b) => a ** b

console.log(power(2, 3)) // it works